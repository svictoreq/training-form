# Training Form por Victor Requena

## Descripción

El formulario fue realizado con HTML, CSS y jQuery.
Se valida con jQuery, aunque adicionalmente, añadí el atributo `require` a todas las entradas en HTML.
